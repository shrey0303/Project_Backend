<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <span class="text-xl font-semibold">Dashboard</span>
                </div>
                
                <div class="flex items-center">
                    <span class="mr-4">Welcome, <%= user.name %></span>
                    <form action="/auth/logout" method="POST">
                        <button type="submit" 
                                class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                            Logout
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="px-4 py-6 sm:px-0">
            <% if (user.role === 'ADMIN') { %>
                <div class="mb-6">
                    <h2 class="text-xl font-bold mb-4">Admin Controls</h2>
                    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
                        <a href="/admin/users" class="block p-6 bg-white rounded-lg shadow hover:shadow-md">
                            Manage Users
                        </a>
                        <a href="/admin/roles" class="block p-6 bg-white rounded-lg shadow hover:shadow-md">
                            Manage Roles
                        </a>
                    </div>
                </div>
            <% } %>

            <div class="bg-white shadow rounded-lg p-6">
                <h2 class="text-xl font-bold mb-4">Your Profile</h2>
                <div class="space-y-4">
                    <p><strong>Email:</strong> <%= user.email %></p>
                    <p><strong>Role:</strong> <%= user.role %></p>
                    <p><strong>Joined:</strong> <%= user.createdAt.toLocaleDateString() %></p>
                </div>
            </div>
        </div>
    </main>
</body>
</html>
